//script

display block на все элементы
Start - включает/выключает видимость(картинка не меняется)
style - можно сделать ссылку и не повторять между скриптами
как сделать выделение двух элементов онклик для сравнению между собой addEventListener+,myProp

//
const f = document.querySelector('.f')
f.myProp = true 

if(f.myProp) {
    console.log(f)
}